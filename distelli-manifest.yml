ech/mono_repo:
  Env:
    - TEST: "4321"
    - THING: "$TEST"
  Build:
  # This section will include the build steps to build the application.   
  # An example
    - echo "hello dumb world $THING"
  Exec:
    - echo "hello dumb world $THING"
