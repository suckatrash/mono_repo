ech/mono_repo:
  PreBuild:
    - cat "90" > test
  Env:
    - THING: "$(cat test)"
  Build:
  # This section will include the build steps to build the application.   
  # An example
    - echo "hello world $THING"
  Exec:
    - echo "hello world $THING"
  PkgInclude:
    - "test"
