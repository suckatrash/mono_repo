ech/mono_repo:
  PreBuild:
    - echo "9001" > /distelli/test_version
  Env:
    - THING: "$(cat /distelli/test_version)"
  Build:
  # This section will include the build steps to build the application.   
  # An example
    - echo "hello world $THING"
  Exec:
    - echo "hello world $THING"
  PkgInclude:
    - "test"
